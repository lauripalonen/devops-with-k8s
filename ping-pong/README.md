# Ping-pong server

Responses with "pong \<n\>" where n = number of requests to `/pingpong` endpoint. 

This service share's an ingress with log-output app. Please see the [ingress.yaml](log-output/manifests/ingress.yaml) in log-output directory to see the ingress file.
