apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-pong
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ping-pong
  template:
    metadata:
      labels:
        app: ping-pong
    spec:
      #volumes:
      #  - name: shared-logs
      #    persistentVolumeClaim:
      #      claimName: logger-claim
      containers:
        - name: ping-pong
          image: palolaur/pingpong:1.14
          #volumeMounts:
          #  - name: shared-logs
          #    mountPath: /usr/src/app/files
